#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - prototypes
 *
 * @dest: pointer
 * @src: pointer
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
